<!--  this is rendering on the show page of the message and it's going to build comments from that message
the first @message is getting the message that we are on
the second @message is building comments in relation to that message-->
<%= simple_form_for([@message,@message.comments.build]) do |f| %>
  <%= f.input :content, label: "Comment" %>
  <%= f.button :submit, :class => "btn-custom" %>
<% end %>
