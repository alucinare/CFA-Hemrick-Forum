<%= simple_form_for @message do |f| %>
  <%= f.input :title, label: "Message Title" %>
  <!-- if no label is give it will give it the name of the title by default -->
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
